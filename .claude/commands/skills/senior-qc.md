Đọc file `.agents/skills/senior-qc/SKILL.md` và áp dụng testing strategy cho task sau.

Ưu tiên: risk-based testing, WebApplicationFactory cho integration test, bao phủ đủ happy path + edge case + negative case + CSRF.

$ARGUMENTS
