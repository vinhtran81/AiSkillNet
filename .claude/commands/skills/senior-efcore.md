Đọc file `.agents/skills/senior-efcore/SKILL.md` và áp dụng EF Core best practices cho task sau.

Kiểm tra kỹ: AsNoTracking() cho read-only, không gọi ToList() sớm, không có N+1 query, đặt Index đúng chỗ.

$ARGUMENTS
