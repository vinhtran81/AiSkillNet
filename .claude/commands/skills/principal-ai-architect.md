Đọc file `.agents/skills/principal-ai-architect/SKILL.md` và thực hiện phân tích kiến trúc AI theo đúng 5 bước trong đó (Khảo sát → Tối ưu → Cắt giảm → Kế hoạch → Phê duyệt).

KHÔNG viết code chi tiết trong bước phân tích. Cuối cùng phải hỏi xác nhận trước khi implement.

$ARGUMENTS
