Đọc file `.agents/skills/senior-logging/SKILL.md` và áp dụng observability guidelines cho task sau.

Dùng structured logging với message template (không string concatenation), thêm Correlation ID, không lộ stack trace trên production.

$ARGUMENTS
