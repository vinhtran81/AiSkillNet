Đọc file `.agents/skills/senior-data-analyst/SKILL.md` và áp dụng analytics guidelines cho task sau.

Naming convention bắt buộc: `object_action` snake_case. Tham chiếu MCP PostHog tại `.agents/master/mcp.md` nếu cần query data.

$ARGUMENTS
