Đọc file `.agents/skills/senior-architect-dotnet/SKILL.md` và áp dụng Clean Architecture guidelines làm framework cho task sau.

Trước khi đề xuất bất cứ thứ gì: kiểm tra Dependency Rule (Domain ← Application ← Infrastructure/Web), không để business logic rò rỉ vào Controller.

$ARGUMENTS
